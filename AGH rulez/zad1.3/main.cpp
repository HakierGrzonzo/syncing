#include<iostream>
using namespace std;

int main(){
	int n;
	cin >> n;
	int numbers[n];
	for(int x = 0; x < n; x++){
		cin >> numbers[x];
	}
	long xorable = numbers[0];
	long sum = xorable;
	for(int x = 1; x < n; x++)
	return 0;
}
